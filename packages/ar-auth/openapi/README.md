# Authrim Auth OpenAPI

This directory contains OpenAPI contracts owned by `@authrim/ar-auth`.

- `auth.openapi.yaml`: authentication, passkey, consent, session, logout, PAR,
  and authorization-flow endpoints owned by the auth Worker.

Do not include private test credentials, production tenant names, or security
finding notes in this directory.
