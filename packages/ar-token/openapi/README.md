# Authrim Token OpenAPI

This directory contains OpenAPI contracts owned by `@authrim/ar-token`.

- `token.openapi.yaml`: OAuth token endpoint and health probes.

Do not include private client secrets or environment-specific credentials.
